(function (W, Backbone) {
    'use strict';

    W.Eval.ItemsModel = Backbone.Model.extend({
        initialize: function (options) {
            this.options = options || {};
        }

    });
}(window, Backbone));
