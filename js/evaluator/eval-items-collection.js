(function (W, Backbone) {
    'use strict';

    W.Eval.ItemsCollection = Backbone.View.extend({
        initialize: function (options) {
            this.options = options || {};
        }
    });
}(window, Backbone));
