describe("Evaluator", function () {
    var evaluatorTests = new WS.EvaluatorControllerTest();

    it("should init evaluator view", evaluatorTests.initCollectionTest);
    it("should init evaluator collection");
    it("should init evaluator state model");
    it("should provide an accessor for updating stateModel('setId')");
});