/*jslint nomen:true*/
/*global window, WS, $, Backbone, _, Test*/

(function (W) {
    "use strict";

    WS.SwitcherViewTest = WS.Test.extend({
        before: function () {
        }
    });
}(window));
