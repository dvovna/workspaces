/*jslint nomen:true*/
/*global WS, $, Backbone, _*/

(function (WS, Backbone) {
    'use strict';

    WS.WorkspacesControllerTest = Backbone.View.extend({
        initializationTest: function () {
            expect(true).toBe(false);
        }
    });
}(WS, Backbone));
